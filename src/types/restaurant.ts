export interface RestaurantData {
    email: string;
    name: string;
    password: string;
    restaurantName: string;
    description: string;
    logoUrl: string;
  }

  export interface Category {
    id: string;
    name: string;
    imageUrl?: string;
  }